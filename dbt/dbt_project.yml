name: duckdb_pipeline
version: "1.0"
config-version: 2
profile: default

model-paths: ["models"]

models:

  duckdb_pipeline:
    +persist_docs:
      relation: true
      columns: true
    staging:
      +materialized: table
      stg_open_meteo:
        +schema: stg_open_meteo